import '../styles/TipResources.module.css'

const TipsResources = (props) => {

    
    return(
        <div className='container'>
           <h4 className={category}>{props.name}</h4>
           <h4>{props.title}</h4>
        </div>
    )
}

export default TipsResources;
    


        
       
    
 